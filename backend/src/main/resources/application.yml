spring:
  datasource:
    url: jdbc:mysql://cs-dept.esm.kr:23306/CatWashHub?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: CatWashHub
    password: Cat@Wash@Hub
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always
      data-locations: classpath:db/data.sql

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  # OAuth2 (나중에 활성화)
#  security:
#    oauth2:
#      client:
#        registration:
#          google:
#            client-id: ${GOOGLE_CLIENT_ID}
#            client-secret: ${GOOGLE_CLIENT_SECRET}
#            scope:
#              - email
#              - profile

jwt:
  secret: ${JWT_SECRET:as429ujfds!@#98s!@XFGd09j1adkogh0-(*#%@((adf1at4jnko0jkp}
  access-expiration: 1800000    # 30분
  refresh-expiration: 604800000 # 7일

server:
  port: 8080
